import { AppFocusDirective } from './app-focus.directive';

describe('AppFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFocusDirective();
    expect(directive).toBeTruthy();
  });
});
